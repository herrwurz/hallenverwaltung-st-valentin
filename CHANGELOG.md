# Changelog

## Unreleased

- chore(obs): Observability MVP — Sentry safe initializer, structured JSON logger, healthcheck endpoint, alerts bridge with HTTP fallbacks (SendGrid/Mailgun/Postmark) and health-check runner. Avoids mandatory SMTP or nodemailer installation on servers. (merged from `feature/obs/sentry`)
